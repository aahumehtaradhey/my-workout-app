# My Workout App 🏋️‍♂️🐍

A lightweight, Command-Line Interface (CLI) based workout tracker written in **Python** and optimized to run directly on Android using **Termux**.

## 🚀 Features

* **Termux Optimized:** Runs flawlessly on Android via Termux without needing a PC.
* **Workout Logging:** Quickly log your daily exercises, sets, reps, and weights.
* **Data Persistence:** Automatically saves your progress in a local file (JSON/TXT).
* **Clean CLI:** Simple and interactive command-line menus.

## 🛠️ Prerequisites

Make sure you have Python installed in your Termux environment:
```bash
pkg install python
